//Template Literals: Multi-line strings and string interpolation using backticks (`).

//Problem: Create a string with dynamic values.

const items = 5;
const price = 100;
console.log(`You bought ${items} items for $${price}.`); 